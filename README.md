### Semantic without any classes
Native-minimal framework uses simple native HTML tags. So you don't need to learn it at all.

### Great styles with just one CSS file
Lightweight with **no** dependencies.

### Built with SCSS
Can be installed as single css or improted as SCSS.

### Light and Dark themes
Shipped with two elegant and minimal color themes, automatically enabled according to the user preference.